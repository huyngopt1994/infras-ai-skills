apiVersion: v1
kind: Service
metadata:
  name: {{ include "minimal-web-app.fullname" . }}
  labels:
    {{- include "minimal-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minimal-web-app.selectorLabels" . | nindent 4 }}
